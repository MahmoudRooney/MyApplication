<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.egypt2.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sign_up" />
        <activity android:name=".rest_password" />
        <activity android:name=".menu" />
        <activity android:name=".my_profile" />
        <activity android:name=".books" />
        <activity android:name=".followers" />
        <activity android:name=".activity" />
        <activity android:name=".setting" />
        <activity android:name=".rest_pass" />
        <activity android:name=".parsonal" />
        <activity android:name=".books_post"></activity>
    </application>

</manifest>